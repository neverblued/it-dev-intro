{
  "name": "it-dev-intro",
  "version": "0.0.1",
  "description": "example project",
  "authors": [
    {
      "name": "Dmytro Pinskyi",
      "url": "http://neverblued.info/"
    }
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/neverblued/it-dev-intro"
  },
  "scripts": {
    "test": "mocha --recursive ./test/*",
    "style": "./node_modules/.bin/jscs .",
    "cover": "./node_modules/.bin/istanbul cover npm run-script test",
    "clean": "./node_modules/.bin/istanbul check-coverage && rm -rf coverage"
  },
  "engines": {
    "node": ">= 0.10.0"
  },
  "devDependencies": {
    "chai": "*",
    "istanbul": "*",
    "jscs": "*",
    "mocha": "*"
  }
}
